#langchain
GROQ_API_KEY=
MODEL_PROVIDER=groq:qwen/qwen3-32b

#get these from the oauth credentials json file
CLIENT_ID=
PROJECT_ID=
AUTH_URI=
TOKEN_URI=
AUTH_PROVIDER_X509_CERT_URL=
CLIENT_SECRET=
REDIRECT_URIS=["http://localhost"]

#environment (dev or prod), used especially for logging config (see config/settings.py)
ENV=dev